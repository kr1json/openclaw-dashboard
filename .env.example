# openclaw-dashboard environment sample

# HTTP server port
DASHBOARD_PORT=7000

# OpenClaw base directory (contains agents/, cron/, config, etc.)
OPENCLAW_DIR=/home/ubuntu/.openclaw

# Workspace root used by the dashboard
WORKSPACE_DIR=/home/ubuntu/.openclaw/workspace

# Optional fallback for WORKSPACE_DIR
OPENCLAW_WORKSPACE=/home/ubuntu/.openclaw/workspace

# Agent id to read sessions from
OPENCLAW_AGENT=main

# Recovery token shown/used by dashboard auth
# If omitted, server.js generates a random token on startup.
DASHBOARD_TOKEN=change-me-recovery-token

# Optional MFA secret (Base32). If omitted, server may read from data/mfa-secret.txt.
DASHBOARD_MFA_SECRET=

# Allow HTTP for non-localhost access (NOT recommended in production)
DASHBOARD_ALLOW_HTTP=false

# macOS-only healthcheck URL for dashboard service probe
GATEWAY_DASHBOARD_URL=http://localhost:18789
